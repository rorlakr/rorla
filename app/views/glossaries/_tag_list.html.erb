<%= icon_tags(@glossary.tag_list, 'glossaries')  %>

<% if user_signed_in? %>
  <%= link_to "태그추가", '#', id: 'add_tag_link', class: 'add_tag_link', remote: true %>
  <div id="tag_list_form_of_glossary_<%= @glossary.id %>" class="tag_list_form_of_glossary">
    <%= render "glossaries/dynamic_tag_list_form" %>
  </div>
<% end %>
